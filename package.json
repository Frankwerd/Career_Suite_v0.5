{
  "name": "job-app-simulator",
  "version": "1.0.0",
  "description": "Job application email parser simulator",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {}
}